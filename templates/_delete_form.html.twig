<form method="post" action="{{ path('comment_admin_delete', {'id': comment.id}) }}" onsubmit="return confirm('{{ 'Are you sure you want to delete this item?'|trans }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
    <button class="btn">{{ 'Delete'|trans }}</button>
</form>